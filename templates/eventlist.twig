{% autoescape false %}
{% if project.events %}
  <h3 class="title">{{data.opts.strings.events_next}}</h3>
<div class="events flexRow">
{% for key,event in project.events %}
<div class="event tile tile40 flexColumn blockDark">
  <div class="fitImg">
  <img src="{{options.baseurl}}data/projects/{{path}}/events/{{key}}/cover.jpg" alt="{{key}}" />
</div>
  <h3 class="title">{{event.opts.title}}</h4>
  <div class="eventDesc tileDesc">{{event.opts.desc}}</div>
  <ul class="eventMeta tileMeta">
    <li><span class="emotpack-icon date"></span><span class="metaDetail">{{event.opts.date|localizeddate('long', 'none')}}</span></li>
    <li><span class="emotpack-icon time"></span><span class="metaDetail">{{event.opts.time}}</span></li>
    <li><span class="emotpack-icon venue"></span><span class="metaDetail">{{event.opts.venue}}</span></li>
    <li><span class="emotpack-icon price"></span><span class="metaDetail">{{event.opts.price}}</span></li>
  </ul>
  {% if today <= event.opts.timestamp %}
  <a href="{{options.baseurl}}?ref={{key}}" class="cta">{{project.opts.strings.buy}}</a>
  {% endif %}
</div>
{% endfor %}
</div>
{% endif %}
{% endautoescape %}
